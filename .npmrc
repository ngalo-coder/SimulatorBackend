legacy-peer-deps=true
no-optional=true
save-exact=false
engine-strict=false
fund=false
audit=false

# Force specific versions to avoid conflicts
force=true

# Disable package-lock generation during problematic installs
package-lock=false